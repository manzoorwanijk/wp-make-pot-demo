import React from 'react';
import { __ } from '@wordpress/i18n';

const Header = () => {
    return (
        <div>
            {__('This is a header.')}
        </div>
    );
};

export default Header;